import Modal from '../ui/Modal';

const TasksDetailsModal = ({ isOpen, setIsOpen }) => {
    return (
        <Modal isOpen={isOpen} setIsOpen={setIsOpen}>

        </Modal>
    );
};

export default TasksDetailsModal;